TASK(pins_reader)
{
	for(unsigned int i = 0; i < N_KEY_HANDLERS; ++i)
	{
		if(digitalRead(key_handlers[i].key) == [#PinActiveLevel])
		{
			(*key_handlers[i].handler)(key_handlers[i].key);
		}
	}
	TerminateTask();
}

